# Session Handoff &mdash; 2026-04-04

## What This Session Did

### 1. Reviewed Inkwren MCP npm Package for Publish

Reviewed `@inkwren/mcp-server` surface area via `/comms mcp-npm-publish-review` in Inkwren session-comms. **Approved for publish.** Key findings:
- 8.7 kB thin bridge, no secrets or business logic &mdash; clean
- 26-tool surface area appropriate (read tools RLS-scoped, write tools double-gated behind feature flag + API key scope)
- Flagged: `.d.ts.map` source paths (confirmed clean by Claude E), `delete_task` needs soft-delete eventually, `import_tasks` limit reduced from 50 to 10 (INK-1218)

Package published as `@inkwren/mcp-server@0.1.0`.

### 2. Updated Config to Use Published npm Package

- Updated `.mcp.json` from local `tsx` source path to `npx @inkwren/mcp-server`
- Changed API URL from `localhost:3003` to `https://app.inkwren.com/api` (production)
- Renamed env var from `INKWREN_AUTH_TOKEN` to `INKWREN_AUTH_TOKEN_BLACKBIRD` (workspace-scoped naming)

### 3. Debugged MCP Connection Issues

Three issues discovered and resolved:

1. **`.mcp.json` ignored:** Claude Code doesn&rsquo;t read `.mcp.json` when a project entry already exists in `~/.claude.json`. Fix: use `claude mcp add-json` to register servers in `~/.claude.json`.

2. **`${VAR}` not expanded:** `~/.claude.json` does NOT expand `${VAR}` references in env values. Fix: use literal key values when adding via `claude mcp add-json`.

3. **`.zshrc` not inherited:** Claude Code&rsquo;s process doesn&rsquo;t always inherit `.zshrc` exports. Fix: export env vars in `~/.zshenv` instead.

Debug thread: `inkwren-develop/internal-docs/session-comms/mcp-npm-connection-debug.md`

### 4. Created `/mcp` Skill

New skill at `.claude/skills/mcp/SKILL.md` for diagnosing and configuring MCP servers. Covers both blackbird-wp and inkwren, with step-by-step diagnostics, common fixes table, and all three gotchas documented.

### 5. Updated CLAUDE.md

- Inkwren MCP section now references npm package (not local source)
- &ldquo;Inkwren MCP Integration&rdquo; no longer says &ldquo;(Future)&rdquo;
- Setup instructions include `.mcp.json` template inline
- Added session startup/end protocols, cross-repo coordination section

### 6. Updated SESSION_NOTES.md

- Persistent notes updated to reflect published package and production URL
- Added pointer to `/mcp` skill for diagnostics

## Immediate Next Step

**Verify Inkwren MCP connection in a fresh session.** The tools loaded successfully (all 18 appeared) but disconnected during a `--resume`. A fresh session (no `--resume`) should confirm end-to-end connectivity. Test with `inkwren_list_workspaces`.

## Remaining Tasks (carried forward)

### Interview Pipeline
- **Need questions sent:** Alicia Cay, Charles Eugene Anderson, Lou J. Berger, Robert Jeschonek
- **Self-interview not written:** Jamie Ferguson

### PUB-6: Rank Math SEO Meta Fields
- Needs PHP snippet from Rookwood, Jamie adds in WP Admin
- Then bulk-set SEO on all 15 Haunted Waters posts

### Other Linear Tickets
- **PUB-7:** Production workflow
- **PUB-8:** Substack MCP integration investigation

## Technical Notes

### MCP Config Gotchas (NEW)
- **Config location:** Always use `claude mcp add-json` &mdash; don&rsquo;t rely on `.mcp.json` when `~/.claude.json` has a project entry
- **Env vars:** `~/.claude.json` does NOT expand `${VAR}` &mdash; use literal values
- **Shell profile:** Use `~/.zshenv` not `.zshrc` for env vars that need to reach Claude Code
- **Full details:** See `/mcp` skill

### Inkwren MCP Server Registration
```bash
claude mcp add-json inkwren --scope local '{"type":"stdio","command":"npx","args":["@inkwren/mcp-server"],"env":{"INKWREN_API_URL":"https://app.inkwren.com/api","INKWREN_AUTH_TOKEN":"ink_actual_key_here"}}'
```
