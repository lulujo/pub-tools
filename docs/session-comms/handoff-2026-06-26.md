# Session Handoff &mdash; 2026-06-26

Long multi-day session covering the back half of the **Escape from 2026 StoryBundle** interview series plus substantial tooling/process work. Bundle **ended 2026-06-25** (~$494.51 pre-refund/tax; Jamie's share 16% as editor+author).

## What Shipped

### Escape from 2026 interviews &mdash; series complete
All interview drafts built (Jamie reviews/publishes). Posts created this session:

| Author / post | Post | Featured | Cover | Tags |
|---|---|---|---|---|
| Kris Rusch &mdash; *What If&hellip; Volume 1* | **5741** | 5739 | 5740 | 393 + 241 |
| Monster Road Trip &mdash; **editors** (Jamie + DeAnna) | **5745** | 5743 | 5744 | 393 + 240 + 235 |
| Monster Road Trip &mdash; **contributors** (Mark/DeAnna/Jamie) | **5749** | 5743 (reused) | 5744 (reused) | 393 + 396 + 235 + 240 |

(Earlier in the run: Jason A. Adams 5715, Annie 5716, Kari 5717, Doug 5733, DeAnna 5734, Michael 5735 &mdash; plus Kathryn 5699, Leslie 5705, Ron 5706 from prior sessions.) New tags this session: Kristine Kathryn Rusch **241**, Mark Leslie **396**. **Jason Chen** never came back with answers &mdash; only `jason_chen_interview_questions.md` exists; no post.

### Tooling (the big build)
- **`integrations/wordpress/md_to_gutenberg.py`** &mdash; the interview converter, now **format-agnostic**: renders the body in document order (headings/paragraphs/lists/dividers + inline marks), so single-author, parallel/joint, and compiled-group posts all work with no code change. Retry-wrapped REST client, idempotent media reuse, slug-exists guard, `verify()` self-checks, `post.yaml` sidecar support, `extra_tag_ids` for multi-author posts, underscore-italics.
- **`integrations/wordpress/test_md_to_gutenberg.py`** &mdash; 54 checks (each a real bug hit this run). Run after any engine change.
- **`.githooks/pre-commit`** &mdash; runs the tests when the engine/tests are staged (`git config core.hooksPath .githooks`).
- **`.claude/skills/post-bundle-interview/`** &mdash; the bundle-interview pipeline skill (sidecar + dry-run + create).

### MCP / sites
- **Inkwren**: all three workspaces wired for Rookwood (`inkwren` = Blackbird, `inkwren-borogrove`, `inkwren-jamie`); Bramble (publishing repo) also has all three. Keys in `.env`.
- **WordPress**: `claude` Editor on all three sites. MCP servers `blackbird-wp`, `borogrove-wp`, `jamie-wp`. Per-site app passwords in `.env`. **Borogrove REST quirk:** site forces trailing slashes &mdash; use `?rest_route=` form (PUB-10).
- **`/mcp` skill** documents cross-project Inkwren setup.

### Process / docs (the flaw fix)
Three interview formats in a row each broke the converter mid-build. Fixed both halves: engine generalized (above), and a **format contract** now documented in three places so it can't recur reactively:
- `~/Dropbox/dev/publishing/protocols/BLOG_POST_FORMATS.md` &rarr; "Supported Structures & Converter Contract" (the document model, the 3 proven shapes, the **flag-novel-block-types-first** rule).
- `/post-bundle-interview` skill points at it.
- Bramble's `CLAUDE.md` Cross-Repo Coordination carries the same pointer + rule (so every Bramble session inherits it).

## Open Follow-ups

- **PUB-6 (Rank Math SEO)** &mdash; *In Progress, blocked on Jamie.* Snippet saved at `integrations/wordpress/rank-math-rest-snippet.php`; Jamie pastes it into the Blackbird child-theme `functions.php` (theme files aren't REST-editable). Verified live this run that `rank_math_*` is NOT yet exposed via REST. Once added, ping Rookwood to run steps 1&ndash;5 (test on one draft &rarr; bundle interviews &rarr; backfill the 15 Haunted Waters posts).
- **PUB-9 (Buffer)** &mdash; filed; social-draft-after-publish workflow to scope. `social.md` files exist per author.
- **PUB-10 (Borogrove trailing-slash)** &mdash; low priority; `?rest_route=` workaround in place. Likely a trailing-slash/canonical plugin on borogrovepress.com (needs an admin to find; `claude` is Editor only).
- **Jason Chen** &mdash; if answers ever arrive, Bramble drops `post.yaml` + `interview_post.md`; building it is a one-liner via `/post-bundle-interview`.

## Technical Notes
- **Regression method:** "byte-identical vs the *live* post" is INVALID &mdash; WordPress normalizes entities (`&mdash;`) back to Unicode (`—`) on save. Compare engine-output vs *saved* engine-output instead, or normalize both sides.
- **Source folders move:** published interview folders get relocated to `authors/published/` &mdash; don't reuse cached `/tmp` configs across days; regenerate paths.
- **Tests must cover the CLI path:** a dry-run broke this run while unit tests passed, because nothing exercised `process_post`. There's now a `process_post` integration test.
- **WPEngine/Cloudflare** throw transient 522/525; the engine retries. UA header required.
