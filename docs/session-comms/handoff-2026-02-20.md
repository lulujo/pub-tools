# Session Handoff — 2026-02-20

## What This Session Did

Completed PUB-2, PUB-3, PUB-4 (Phases 1-3 of the WordPress integration). All marked Done in Linear.

### Key Discoveries

1. **MCP tools work.** 35 tools via `blackbird-wp` server. No `wp_` prefix needed — server name provides namespace.
2. **Curly quotes require HTML entities.** Unicode curly quotes get flattened to straight quotes by the MCP/REST pipeline. Use `&ldquo;` `&rdquo;` `&lsquo;` `&rsquo;` `&mdash;` etc. Documented in CLAUDE.md and SITE_INVENTORY.md.
3. **Content format is Gutenberg blocks.** Existing Blackbird posts use Gutenberg, not Classic blocks. We match that.
4. **CTA patterns work via reference.** `<!-- wp:block {"ref":5400} /-->` for Haunted Waters, `5146` for Haunted Places. Claude can read patterns but not create/edit them (Editor role, 403).
5. **Featured image upload requires REST API.** MCP `create_media` only takes URLs. Local files go through `POST /wp-json/wp/v2/media` with `CLAUDE_BLACKBIRD_WP_PASSWORD` from `.env`. Then attach via MCP `update_post`.
6. **Vellum files are readable.** They're zip archives with plist data. `Author and Collection Information.vellum` has all author bios. See the extraction pattern below.
7. **Linear works.** API key is in `.env`. See CLAUDE.md for the CLI command pattern.

### What's Live on Blackbird

- **Post 5480** (draft): Steve Vernon spotlight — complete with bio from Vellum, featured image, CTA pattern. Only missing the excerpt (verbatim from manuscript, Jamie provides).
- **Media 5482**: Steve Vernon spotlight featured image (1200x628)

### Skills Created

- `/post-spotlight` — full spotlight draft workflow
- `/post-interview` — full interview draft workflow
- `/post-release` — anthology launch post workflow
- `/upload-image` — local file upload + attach as featured image

### Vellum Bio Extraction Pattern

The Vellum file at:
```
~/Dropbox/Jamie/Writing and Publishing/Blackbird Publishing/Publishing/Book files : formatting/Info to reuse/Author and Collection Information.vellum
```

Is a zip containing `content.vellumcontent` (a binary plist). Extract with:
```python
import plistlib, zipfile
with zipfile.ZipFile("path/to/file.vellum") as z:
    with z.open("content.vellumcontent") as f:
        data = plistlib.load(f)
# Then walk the nested dict/list structure searching for author name strings
```

Social links are also in the plist (website, Facebook, Amazon, X, BookBub, Goodreads).

## Task for Next Session

**Create a `/author-info` skill** that extracts author bio and social links from the Vellum file. Should:
1. Take an author name as argument
2. Open the Vellum file, parse the plist
3. Find the author's bio text and social links
4. Return formatted output ready to paste into a WordPress post (Gutenberg blocks with HTML entities)

This eliminates the `[BIO NEEDED]` placeholder from the spotlight/interview workflow entirely. Update the `post-spotlight` and `post-interview` skills to use it.

## What's Next (Linear)

- **PUB-5:** Scheduled posts with timezone handling
- **PUB-6:** Rank Math SEO meta fields (needs PHP snippet on server)
- **PUB-7:** Production workflow — batch test and document
